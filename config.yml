title: MPublic Speaking
tagline: A brand new course on Learning Lab
description: TEST
template:
  repo: public-speaking-template
  name: public-speaking
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
